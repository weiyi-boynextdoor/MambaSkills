# Development Notes

Future improvements:

- Add optional helpers for extracting text from `.docx` and `.pdf` resumes when the workspace commonly contains those formats.
- Add a comparison output mode that writes a short change log beside the tailored resume.
- Add reusable ATS keyword audit guidance if users repeatedly ask for scoring or gap analysis.
- Add bilingual handling notes if resume and job description languages differ often.
